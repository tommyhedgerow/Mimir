---
type: map
subject:
state: in-progress          # in-progress | paused | complete | index
opened:
frontier:                   # 一行 —— 这条脉络实际走到哪
next:                       # 一行 —— 下一个节点
nodes: {}                   # 图节点 id → 概念笔记，一旦有了的话
graph:                      # 主干：结构住在这里，不在图里
  goal: "G | 这条脉络是为了什么"
  foundations:
    - "F1 | 它架在其上的那条无条件真理"
  nodes:                    # 计划中的，背后还没有概念笔记
    - "A | 第一个节点"
  edges:
    - F1 -> A
    - A -> G
tags:
  - learn
  - moc
updated:
---

# {{title}}

> 这条脉络，按依赖顺序：什么架在什么上，以及当前的前沿在哪里。不是一切东西的清单——只有承重的那些概念。

## 根基

_这条脉络的无条件真理——或者，诚实地说，是它实际架在其上的那些定义、区分和约束。哪里没有干净的根基，就注明。_

- [[]] ——
- [[]] ——

## 按依赖顺序

1. [[]] ——
2. [[]] ——
3. [[]] ——

## 依赖图

_画出来的，不是写出来的。`Tools/vault-chart.mjs` 从上面那个 `graph:` 主干连同概念笔记把它渲染到 `Learn/Viz/<filename-slug>.svg` —— 所以节点上的颜色就是那条概念自己的状态：**琥珀色**学习中，**红色**不稳固，**绿色**已确立，灰色是还只是计划的节点。任何东西挪动之后，用 `node Tools/vault-chart.mjs` 重新生成。一条就是普通链的主干会被画成列表而不是图，因为一条链本来就是一张列表。_

![[<filename-slug>.svg]]

## 前沿

_我们走到了哪里，下一个是什么。在这个脉络里规划一次会话之前先读这里。_

- **已到达**：
- **下一个**：
- **不稳固**：_（讲过但不稳固——往上搭之前先检查）_

## 这门学科在哪里有分歧

_有争议的点，以及各方的阵营。值得一直看得见：一张假装这门学科已经有定论的图谱，教的是错的东西。_
