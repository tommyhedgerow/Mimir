---
type: dashboard
tags:
  - learn
  - dashboard
updated:
---

# 概览

> 我自己的状态，从笔记里读出来，而不是重新打一遍。下面每张表都是对 `Learn/` 属性的实时视图——这里没有一样东西是手工维护的，所以这里也没有一样东西能悄悄和它来自的笔记不符。
>
> 视图定义在 `Learn/Dashboard.base` 里。要加一个或改一个，打开那个文件（或者用下面任意一张表里的视图菜单）。

## 什么到期了

![[概览.base#到期]]

_天数倒数到到期日；负数就是逾期。_

## 我掌握什么，以及有多稳固

![[概览.base#概念]]

_按 `status` 分组。**`fragile` 是一个标记，不是一个状态** —— 一个不稳固的概念在它的脉络图上画成红色，它的意思是：在往上搭任何东西之前，先冷着把它提取一遍。现在还没有任何标记，因为还没有讲过任何东西。_

## 各个脉络

![[概览.base#脉络]]

_每个脉络一行：它走到哪、前沿是什么、下一个是什么。这是图谱的属性，所以图谱一动它就动。第一个学科在 `Learn/Maps/` 里有了图谱之前，这张表一直空着。_

## 会话

![[概览.base#会话]]

_`Probe %` 是你在夹逼探测里答对的比例。**低分是一次瞄得准的探测，不是一次糟糕的会话** —— 全对的探测说明题太浅，边界根本没找到。_

## 提取日志

![[概览.base#提取日志]]

_空的，而且空得诚实：还没有提取过任何东西。一次复习会话之后，`retrievals:` 写进概念笔记的那一刻它就填上了——而这是这里唯一一张终究会显示这些有没有留住的表。_

---

**图表。** [Bases Chart Views](https://github.com/haiqiang-zhang/obsidian-bases-charts) 已经装好，它在这些同样的表上加了折线、柱状、散点和饼图视图。等提取日志里不止一行的时候值得做一个——在那之前没有什么可画的。
